; The fixture emits its own junk on demand, through control.php, rather than by having PHP scatter warnings into its
; own output. Keeping display_errors off means a test which asks for junk gets exactly the junk it asked for.
display_errors = Off
log_errors = On
error_reporting = E_ALL
max_execution_time = 60
memory_limit = 256M
